apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-edge-cache
  namespace: netflex-edge
  labels:
    app: redis-edge-cache
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-edge-cache
  template:
    metadata:
      labels:
        app: redis-edge-cache
    spec:
      containers:
      - name: redis
        image: redis:7.2-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-edge-service
  namespace: netflex-edge
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: redis-edge-cache
